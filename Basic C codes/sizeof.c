#include"../include/sumit.h"
void size_of()
{
    int a;
    float b;
    char c;
    double d;
    long int l;
    short int l2;
    printf("\n\nsize of integer variable %d",sizeof(a));
    printf("\nsize of float variable %d",sizeof(b));
    printf("\nsize of char variable %d",sizeof(c));
    printf("\nsize of double variable %d",sizeof(d));
    printf("\nsize of long integer variable %d",sizeof(l));
    printf("\nsize of short integer variable %d",sizeof(l2));

}